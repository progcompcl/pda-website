---
const { title, class: customClass = "" } = Astro.props;
---

<div
  class={`not-prose rounded-sm shadow-sm border-1 border-black/15 pt-2 px-4 pb-4 ${customClass}`}
>
  {title && <h3 class="text-2xl my-2 font-bold">{title}</h3>}
  <div class="border-t border-gray-300 my-2"></div>
  <div class="prose-sm md:prose dark:prose-invert text-base"><slot /></div>
</div>
